)
library(WDI)
library(dplyr)
library(tidyr)
library(ggplot2)
# --- 1) Indicateurs WDI ---
inds <- "IT.CEL.COVR.ZS"
# --- 2) Télécharger pour la RDC ---
mobile_raw <- WDI(
country   = "CD",         # RDC
indicator = inds,
start     = 2000,
end       = 2023,
extra     = TRUE
)
library(WDI)
# Rafraîchir le cache local des indicateurs
cache <- WDIcache()
options(timeout = 1000)
library(WDI)
# Rafraîchir le cache local des indicateurs
cache <- WDIcache()
# Vérifier que l’indicateur existe dans le cache
WDIsearch("IT.CEL.COVR.ZS", cache = cache)
# Télécharger en utilisant ce cache actualisé
dat <- WDI(country = "CD", indicator = "IT.CEL.COVR.ZS",
start = 2000, end = 2023, extra = TRUE, cache = cache)
library(WDI)
library(dplyr)
library(tidyr)
library(ggplot2)
# --- 1) Indicateurs WDI ---
inds <- "SI.LSH.TOTL.ZS"
# --- 2) Télécharger pour la RDC ---
mobile_raw <- WDI(
country   = "CD",         # RDC
indicator = inds,
start     = 2000,
end       = 2023,
extra     = TRUE
)
library(WDI)
library(dplyr)
library(tidyr)
library(ggplot2)
# --- 1) Indicateurs WDI ---
inds <- "EN.POP.SLUM.UR.ZS"
# --- 2) Télécharger pour la RDC ---
mobile_raw <- WDI(
country   = "CD",         # RDC
indicator = inds,
start     = 2000,
end       = 2023,
extra     = TRUE
)
head(mobile_raw)
library(WDI)
library(dplyr)
library(tidyr)
library(ggplot2)
# --- 1) Indicateurs WDI ---
inds <- "VC.IHR.PSRC.P5"
# --- 2) Télécharger pour la RDC ---
mobile_raw <- WDI(
country   = "CD",         # RDC
indicator = inds,
start     = 2000,
end       = 2023,
extra     = TRUE
)
head(mobile_raw)
library(WDI)
library(dplyr)
library(tidyr)
library(ggplot2)
# --- 1) Indicateurs WDI ---
inds <- "SP.REG.BRTH.ZS"
# --- 2) Télécharger pour la RDC ---
mobile_raw <- WDI(
country   = "CD",         # RDC
indicator = inds,
start     = 2000,
end       = 2023,
extra     = TRUE
)
head(mobile_raw)
library(WDI)
library(dplyr)
library(tidyr)
library(ggplot2)
# --- 1) Indicateurs WDI ---
inds <- "GC.REV.XGRT.GD.ZS"
# --- 2) Télécharger pour la RDC ---
mobile_raw <- WDI(
country   = "CD",         # RDC
indicator = inds,
start     = 2000,
end       = 2023,
extra     = TRUE
)
head(mobile_raw)
library(WDI)
library(dplyr)
library(tidyr)
library(ggplot2)
# --- 1) Indicateurs WDI ---
inds <- "BX.KLT.DINV.WD.GD.ZS"
# --- 2) Télécharger pour la RDC ---
mobile_raw <- WDI(
country   = "CD",         # RDC
indicator = inds,
start     = 2000,
end       = 2023,
extra     = TRUE
)
head(mobile_raw)
library(WDI)
library(dplyr)
library(tidyr)
library(ggplot2)
# --- 1) Indicateurs WDI ---
inds <- "BX.TRF.PWKR.DT.GD.ZS"
# --- 2) Télécharger pour la RDC ---
mobile_raw <- WDI(
country   = "CD",         # RDC
indicator = inds,
start     = 2000,
end       = 2023,
extra     = TRUE
)
head(mobile_raw)
library(WDI)
library(dplyr)
library(tidyr)
library(ggplot2)
# --- 1) Indicateurs WDI ---
inds <- "IT.NET.USER.ZS"
# --- 2) Télécharger pour la RDC ---
mobile_raw <- WDI(
country   = "CD",         # RDC
indicator = inds,
start     = 2000,
end       = 2023,
extra     = TRUE
)
head(mobile_raw)
library(WDI)
library(dplyr)
library(tidyr)
library(ggplot2)
# --- 1) Indicateurs WDI ---
inds <- "SP.RUR.TOTL.IN.ZS"
# --- 2) Télécharger pour la RDC ---
mobile_raw <- WDI(
country   = "CD",         # RDC
indicator = inds,
start     = 2000,
end       = 2023,
extra     = TRUE
)
library(WDI)
library(dplyr)
library(tidyr)
library(ggplot2)
# --- 1) Indicateurs WDI ---
inds <- "SP.RUR.TOTL.ZS"
# --- 2) Télécharger pour la RDC ---
mobile_raw <- WDI(
country   = "CD",         # RDC
indicator = inds,
start     = 2000,
end       = 2023,
extra     = TRUE
)
head(mobile_raw)
library(WDI)
library(dplyr)
library(tidyr)
library(ggplot2)
# --- 1) Indicateurs WDI ---
inds <- "SP.RUR.TOTL"
# --- 2) Télécharger pour la RDC ---
mobile_raw <- WDI(
country   = "CD",         # RDC
indicator = inds,
start     = 2000,
end       = 2023,
extra     = TRUE
)
head(mobile_raw)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SE.XPD.TOTL.GB.ZS"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2023,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SE.XPD.TOTL.GD.ZS"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2023,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
install.packages("ilostat")
install.packages("Rilostat")
library(ilostat)
library(Rilostat)
library(dplyr)
# Lister les jeux disponibles
# ilostat::search_ilostat("social protection expenditure")
# Exemple (ID fictif, à remplacer par le code réel dans ILOSTAT)
id_sdg131_exp <- "SDG_0131_SOCX_GDP_A"  # Dépenses protection sociale en % PIB (exemple)
# Télécharger pour la RDC
socprot_exp <- get_ilostat(id = id_sdg131_exp, time_format = "num") %>%
filter(ref_area == "COD") %>%
arrange(time)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SL.TLF.CACT.ZS"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2023,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "WDI:SI.POV.GINI"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2023,
extra     = TRUE
)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SI.POV.GINI"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2023,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "DT.ODA.ALLD.CD.PC"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2023,
extra     = TRUE
)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SM.POP.NETM"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2023,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SH.H2O.SMDW.ZS"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2023,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SN.ITK.DEFC.ZS"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2023,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SN.ITK.MSFI.ZS"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2023,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SN.ITK.MSFI.ZS"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2025,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SN.ITK.SVFI.ZS"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2025,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SH.STA.WAST.ZS"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2025,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SH.STA.OWGH.ZS"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2025,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "AG.AGR.TRAC.NO"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2025,
extra     = TRUE
)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SI.POV.MDIM.XQ"  # Dépenses publiques d'éducation (% du PIB)
# --- Télécharger pour la RDC ---
edu_spend <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 1990,
end       = 2025,
extra     = TRUE
)
# --- Aperçu ---
head(edu_spend)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "per_si_allsi.cov_pop_tot"  # Couverture protection sociale (% population)
# --- Télécharger pour la RDC ---
socprot <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 2020,
end       = 2025,
extra     = TRUE
)
# --- Aperçu ---
head(socprot)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SH.STA.BRTC.ZS"  # Couverture protection sociale (% population)
# --- Télécharger pour la RDC ---
socprot <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 2020,
end       = 2025,
extra     = TRUE
)
# --- Aperçu ---
head(socprot)
library(WDI)
library(dplyr)
library(ggplot2)
# --- Indicateur WDI ---
indicator <- "SH.FPL.SATI.ZS"  # Couverture protection sociale (% population)
# --- Télécharger pour la RDC ---
socprot <- WDI(
country   = "CD",         # RDC
indicator = indicator,
start     = 2020,
end       = 2025,
extra     = TRUE
)
# --- Aperçu ---
head(socprot)
install.packages("ilostat")
# installe le package 'remotes' si besoin
install.packages("remotes")
# installe ilostat directement depuis GitHub
remotes::install_github("InseeFrLab/ilostat")
library(imfr)
library(imf.data)
# Liste des datasets IMF
imf_datasets()
library(imf.data)
# Liste des datasets IMF
imf.data_datasets()
# scripts/run.R
suppressPackageStartupMessages({
library(dotenv); library(targets)
})
if (file.exists("config/.env")) dotenv::load_dot_env("config/.env", override = TRUE)
tar_make()
library(plumber)
install.packages("plumber")
library(plumber)
library(DBI)
setwd("D:/Documents/Poste_UN/Projet_Automatisation/onu_rdc_dashboard/api")
library(plumber)
pr("plumber.R")$run(host="127.0.0.1", port=8000)
source("run_api.R")
setwd("D:/Documents/Poste_UN/Projet_Automatisation/onu_rdc_dashboard/api")
library(plumber)
source("run_api.R")
source("run_api.R")
